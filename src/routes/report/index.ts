import attendance_report from './attendance_report';
import leave_balance_report from './leave_balance_report';

const report = [
  leave_balance_report,
  attendance_report,
];

export default report;
